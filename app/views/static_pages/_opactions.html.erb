<% if @op == OP_ADDITION %>
<% options = ['Numerator is bigger than Denominator'] %>
<%elsif @op == OP_SUBSTRACTION%>
<%options = ['Numerator is bigger than Denominator', 'Include double borrowing'] %>
<%elsif @op == OP_MULTIPLICATION%>
<%options = ['Numerator is bigger than Denominator']%>
<%elsif @op == OP_DIVISION%>
<%options = ['Numerator is bigger than Denominator', 'Allow Fractions']%>
<%else%>
<%options = ['invalid']%>
<%end%>

<div class="span4"> </div>

<div class="span4">

<table class="table table-bordered">
<% for j in (0..options.size-1) %>
        <% elid = "option" + j.to_s%>

          <tr>
            <td>
              <div class="modifer">
                <%= radio_button_tag elid, 'no' %>
              </div>
            </td>
            <td>
              <div class="modifier-text">
                <%= label_tag elid, options[j] %>
              </div>
            </td>
          </tr>
<%end%>
</table>
</div>

<div class="span4"> </div>

